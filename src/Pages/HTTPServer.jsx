const Home = () => {
    return ( 

        <div>
            <h1>Welcome to the HTTP Server</h1>
            <p>This is a simple HTTP server application made with C#. It allows you to serve static files and handle basic HTTP requests.</p>
        </div>
            );
};

export default Home;